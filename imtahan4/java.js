function uniqueLength(nums) {
    if(nums.length === 0) {
        return 0;
    }
    let i=0;
  return
    while(nums[j] == nums[i]) {
        if(let j=1; j < nums.length !==) {
            i++;
            nums[i] = nums[j];
            j++;
        } else {
            j++;
        }
    }
    nums[i] i+1;
}